---
interface Props {
    id: string;
    imagen: string;
}

const { id, imagen } = Astro.props;
---

<div class="brand-card glass-card fade-in-up">
    <div class="card-image-wrap">
        <img src={imagen} class="card-image" alt={`Marca ${id}`} />
    </div>
</div>

<style>
    .brand-card {
        padding: var(--spacing-xs);
        align-items: center;
        justify-content: center;
        min-height: 100px;
    }

    .card-image-wrap {
        width: 100%;
        aspect-ratio: 3 / 2;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0;
        min-height: 80px;
    }

    .card-image {
        max-width: 100%;
        max-height: 100%;
        object-fit: contain;
    }
</style>
